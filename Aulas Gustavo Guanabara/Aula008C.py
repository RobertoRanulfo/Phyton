from math import sqrt, floor
num = int(input('Digite um número '))
raiz = sqrt(num)
print('A raiz de {} é igual a {}'.format(num, floor(raiz)))
# nesse caso fizemos o from depois a biblioteca e escolhemos as fórmulas que iríamos precisar
# assim não precisamos mais citar o nome da biblioteca todas as vezes






