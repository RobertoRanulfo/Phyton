from math import trunc
num = float(input('Digite um número '))
print('O número digitado é {} e sua parte inteira é {}'.format(num, trunc(num)))
# Nesse caso usamos o from math import trunc para escolher apenas a fórmula trunc da biblioteca
